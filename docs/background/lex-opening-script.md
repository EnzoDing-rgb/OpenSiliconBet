# Lex 开场贯口（阶段 0.5 · 固定文案）

面向「RISC-V 三国杀」demo：Lex Fridman 主持口吻、**全程中文**、**不念股价数字**（产业关系用比喻讲清即可）。

## 预生成与音色（实现约定）

| 项 | 约定 |
|----|------|
| 长版音频 | `frontend/public/audio/lex-opening-long.mp3` |
| 短版音频（Skip） | `frontend/public/audio/lex-opening-short.mp3` |
| 合成 API | DashScope TTS（与仓库现有链路一致） |
| **本阶段 `voice_id`** | **`VOICE_ID_LEX`**（推荐；与实时论坛 TTS 同一克隆）。未配置时可退回 **`VOICE_ID_MEARSHEIMER`** |
| 预生成命令 | 仓库根目录：`.venv/bin/python scripts/pregen_lex_opening.py`（读上文 Markdown 文案 → 写 `frontend/public/audio/*.mp3`，依赖 `DASHSCOPE_API_KEY` + `ffmpeg`） |
| 前端 | `OpeningPlayer`：默认播 long；用户点 **跳过** → 停止 long，**立即**播 short，结束后进入论坛交锋（阶段 1） |

文案变更后需重新跑 `scripts/pregen_lex_opening.py` 生成上述两个 mp3。

---

## 阶段 2.5 · 转场口播（黄仁勋结束后 → 观众提问）

**文案钉死（一字不改，除非产品负责人书面改需求）**：

> 好，观众朋友们，我们现在进入观众提问环节。

- **时机**：黄仁勋阶段 2 独白 **整段播放完成**（`onended` 或等价）之后；**之前**不得开放主屏提问输入。
- **实现**：可与 0.5 一样预合成 mp3，或运行时 TTS；`voice_id` 过渡期仍可用 **`VOICE_ID_MEARSHEIMER`**。详见 [`docs/design/architecture.md`](../design/architecture.md) §1。

---

## 长版（约 60 秒朗读量）

大家好，我是 Lex。

很多人第一次听到「指令集」三个字会觉得离自己很远——好像只有工程师才该关心。但我想用一个特别笨的比喻把它拉近一点：

想象三条**高速公路**。第一条修得最早，收费站最多、服务区最全，几乎所有大货车都习惯走它——这就是 **x86**，几十年的世界默认通道。第二条路后来居上，把「省电」和「无处不在」做成了艺术，你的手机、平板、车里很大一部分都在用它——这是 **ARM**。第三条路是新来的：它说「规则写在纸上，谁都能修路、谁都能改收费站」——开源、模块化、不被某一家锁死——这就是 **RISC-V**。

三条路不是谁把谁「一脚踢死」的故事，更像一场长期的**共存与拉扯**。而今天坐在我旁边的几位朋友，恰好分别站在这三条路上——但他们又都被同一件事绑在一起：**AI 和 Agent 把算力饥渴写进了每一个产品里**。所以我想问的问题其实不炫技，而是很朴素：**在这波浪潮里，RISC-V 到底是配角，还是有机会成为主角之一？**

我不站队。我只负责把问题问得更深一点。接下来，让我们听听路上的人自己怎么说。

---

## 短版（用户点击网页「跳过」后立刻播放）

全文如下（宜控制在 **5 秒**内念完；语气干脆、友好）：

> 好的，那让我们直接开始。

---

## 字幕条（可选，与长版对齐分行）

若前端做逐句字幕，可按长版自然断句切行；短版仅一行即可。
